# 1C exports are windows-1251 with CRLF line endings. Treat them as binary so
# git never re-encodes or normalises them: the committed fixtures must stay
# byte-identical to what the generator produces.
data/feeds/*.csv binary
